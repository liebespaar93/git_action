name: Hello world

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Run pwd
      run: 
        pwd
        cd ../
        ls -al
        cd ../
        ls -al
        cd ../
        ls -al
